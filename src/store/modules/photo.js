const state = {
  recording: null
}

const getters = {
}

const actions = {
}

const mutations = {
  START_RECORDING (state) {
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}

if (process.env.NODE_ENV !== 'production') {
}
